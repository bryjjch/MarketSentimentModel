# Copy to .env.local and set to your API Gateway HTTP API base URL (no trailing slash).
# Example: terraform output -raw http_api_invoke_url
VITE_API_BASE_URL=https://xxxxxxxx.execute-api.us-east-1.amazonaws.com
