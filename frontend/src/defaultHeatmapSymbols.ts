/**
 * Default symbols shown on the heatmap on first load.
 * Kept in sync with Terraform `top_tickers_json` default (`variables.tf`).
 */
export const DEFAULT_HEATMAP_SYMBOLS = [
  'AAPL',
  'MSFT',
  'GOOGL',
  'META',
  'NVDA',
] as const
