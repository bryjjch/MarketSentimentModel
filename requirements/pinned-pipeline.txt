# Pin SageMaker Python SDK to v2.x for building and upserting the training pipeline.
# v3+ splits/removes the legacy `sagemaker.workflow.*` API used by
# `src/sagemaker/pipeline/build_pipeline.py` and `pipeline_definition.py`.
#
# This is the pipeline-submitting *client* environment, and it is deliberately
# separate from pinned-train.txt. They cannot be installed together: sagemaker 2.x
# requires either pandas>=2.3.0 or numpy<2, both of which contradict the training
# pins. Nothing needs them together -- the training container never has the SDK.
#
# Install (from repo root, ideally Python 3.10-3.12):
#   pip install -r requirements/pinned-pipeline.txt
#
# Or: pip install -e ".[pipeline]"

sagemaker>=2.200,<3
boto3>=1.28.0
